id: Info
name: Info
description: |
  An informational entry with no special fields

typeIdentification: 
  - ".*\\(\\s*i\\s*\\).*"       # Contains (i) somewhere in the name
  
allowedSubTypes: []                # No subtypes allowed

fields: {}                         # No special fields
