
### Data format

/data/demo = demo database

each instance of a type is a new markdown file with yml front matter

Files and folders are the same: MY_ENTRY.md can be written as MY_ENTRY/-this.md (used when it has sub objects)
("-this" is defined in data/demo/.sys/config.yml, could be renamed)

Entries in the hierarchie may also be included (linked) in a different place in the hierarchie

Initial data

- Demo process on first level (Activity) (has a few activity levels levels and info entries)
- Persons: put demo persons here (linked from Activity)

### Code

The app uses abstract renderers to be able to render multiple types of entries and provide entry specific function.

- Modular type system types/MY_TYPE
  - /ajax
  - config.yml
    - e.g. type definition: speaking-identifier_YYYY-MM-DD-HHMM, description, ...
    - definitions: hierarchical data fields
    - requirements e.g. libaries
  - controller.php
  - list_renderer.php
  - detail_renderer.php (edit)
  - mobile_renderer.php (mobile cell renderer)
  - styles.css
  - /themes
  - controller.js
  - maybe more as needed...

Write classes.

Initial types

- Activity = just a text entry with headline and description
- Info = just a text entry with headline and description
- Person (for participants)
- File (for resources)

### UI

```
-------------------------------------------------------
| NAME    [edit] | NAME_2                    [AI] ... |  <-- app header
-----------------|------------------------------------|
| TABS       [+] |                       |        + v |  <-- Second (small) AI header (AI sidebar only)
| TOOLS          |                       |------------|
| LIST           | EDIT                  |            |
| ...            |                       |            |
|                |                       | AI sidebar |
|                |                       |            |
|                |                       |            |
|                |                       |            |
|                |                       |            |
|----------------|                       |------------|
| FOO            |                       | [     ][ ] |  <-- Input and send button
-------------------------------------------------------
```

3 col content area layout:

- In principle the layout use a 2 main columns layout
  - Left col: navigation
  - Content area may be devided further
    - Mid col: edit
    - Right col: AI sidebar

- "App header"
  - The app header line has the same design over all main columns
  - Its content is column specific
  - Commonly used things in app header
    - [AI] = unfold AI sidebar
    - ... = dropdown menu
      - entry specific entries
      - devider
      - commonly used entries

- Left column
  - Layout
    - In app header line: Name of the currenly visible entry form the hierarchie (edit button right aligned)
    - TABS:
      - Which tabs are available is defined in the type of the currently visible object
        - for type "Activity" it is: "Entries" (sub entries), "Participants", "Resources" (files)
      - aligned to the right: Add dropdown for new entry
    - TOOLS: Visible for tab "Entries" only: toolbar e.g. filters for the list
    - List of rendered entries
      - list entries
        - "cards" with rendered layout per type (no gap/margin) 
        - CRUD functions like delete
      - end of list: load more functionallity (button)
    - FOO: we can put some buttons or states there in case no space elsewhere (or leave empty for now)
  - navigation
    - second level directly unfoldable
    - third and deeper levels in herarchie: navigate to sub level on click or touch
- Mid column: rendered detail editor per type
  - in app header:
    - this area has a default rendering: NAME_2 is name of currently selected entry ("Current" in case "[edit]" was clicked)
  - EDIT: Remaining Edit content area rendered by type renderer
    - may also support tabs or have a fooder area
- AI sidebar: UI dummy for now, implementation later, unfoldable with [AI]
  - second (small) header below app header (plus = new session, "v" = unfold session list)
  - Input and send area

Find a good looking width for each of the 3 sidebars. First columns can't be too narrow in case we have many attributes in rendered list entries we must be able to show it there.

Make th UI work on mobile decives as well.

### Design

IMPORTANT: Make a nice design that looks great.

- /styles = default styles
- /themes/default.css = first theme
